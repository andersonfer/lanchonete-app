---
# PersistentVolumeClaim para MySQL Cozinha
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-cozinha-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard

---
# Service para MySQL Cozinha (Headless Service)
apiVersion: v1
kind: Service
metadata:
  name: mysql-cozinha-service
  namespace: default
  labels:
    app: mysql-cozinha
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app: mysql-cozinha
  clusterIP: None

---
# StatefulSet para MySQL Cozinha
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-cozinha
  namespace: default
  labels:
    app: mysql-cozinha
spec:
  serviceName: mysql-cozinha-service
  replicas: 1
  selector:
    matchLabels:
      app: mysql-cozinha
  template:
    metadata:
      labels:
        app: mysql-cozinha
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
          name: mysql
        envFrom:
        - secretRef:
            name: mysql-cozinha-secret
        env:
        - name: MYSQL_ROOT_HOST
          value: "%"
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          exec:
            command:
            - mysqladmin
            - ping
            - -h
            - localhost
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          exec:
            command:
            - bash
            - -c
            - mysql -h localhost -u root -p$MYSQL_ROOT_PASSWORD -e 'SELECT 1'
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 2
      volumes:
      - name: mysql-data
        persistentVolumeClaim:
          claimName: mysql-cozinha-pvc
